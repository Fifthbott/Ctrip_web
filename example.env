NODE_ENV=production
PORT=3000

# 数据库配置
DB_HOST=mysql
DB_USERNAME=user1
DB_PASSWORD=qwe123
DB_NAME=Ctrip

# JWT配置
JWT_SECRET=your_secret_key_change_this_in_production
JWT_EXPIRES_IN=7d

# 上传文件配置
UPLOAD_PATH=src/uploads
MAX_FILE_SIZE=100000000
BASE_URL=http://localhost

# 是否在启动时同步表结构（生产环境通常设为false）
SYNC_MODELS=false 